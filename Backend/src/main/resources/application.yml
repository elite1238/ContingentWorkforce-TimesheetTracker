spring:
  application:
    name: WF
  datasource:
    url: jdbc:postgresql://localhost:5432/workforce_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET:change-me-not-a-real-secret}
  expiration-ms: 86400000

server:
  port: 8080

resend:
  api-key: ${RESEND_API_KEY:}
  from: ${RESEND_FROM:vendor@projectbuild.me}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
